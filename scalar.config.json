{
  "subdomain": "scalar-docs-mermaid",
  "slug": "scalar-docs-mermaid",
  "guides": [
    {
      "name": "Demo",
      "sidebar": [
        { "path": "docs/guides/mermaid.md", "type": "page", "name": "Mermaid" }
      ]
    }
  ],
  "siteConfig": {
    "bodyScript": "(function(){console.log('[Mermaid] Script wrapper executing');const waitForMermaidElements=(callback,maxAttempts=50,interval=100)=>{let attempts=0;const check=()=>{const elements=document.querySelectorAll('pre.mermaid,code.mermaid');console.log('[Mermaid] Checking for mermaid elements, found:',elements.length);if(elements.length>0||attempts>=maxAttempts){callback(elements.length>0);}else{attempts++;setTimeout(check,interval);}};check();};const init=()=>{console.log('[Mermaid] Init function called, readyState:',document.readyState);try{const s=document.createElement('script');console.log('[Mermaid] Script element created');s.type='module';s.textContent=`import mermaid from 'https://cdn.jsdelivr.net/npm/mermaid@11/dist/mermaid.esm.min.mjs';console.log('[Mermaid] Module loaded');mermaid.initialize({startOnLoad:false});console.log('[Mermaid] Mermaid initialized');const runMermaid=()=>{const elements=document.querySelectorAll('pre.mermaid,code.mermaid');console.log('[Mermaid] Found',elements.length,'mermaid elements');if(elements.length===0){console.warn('[Mermaid] No mermaid elements found, retrying...');setTimeout(runMermaid,200);return;}try{mermaid.run();console.log('[Mermaid] Mermaid.run() executed successfully');}catch(e){console.error('[Mermaid] Error in mermaid.run():',e);}};runMermaid();`;s.onerror=(e)=>{console.error('[Mermaid] Script error:',e);};s.onload=()=>{console.log('[Mermaid] Script loaded successfully');};document.head.appendChild(s);console.log('[Mermaid] Script appended to head');waitForMermaidElements((found)=>{if(found){console.log('[Mermaid] Mermaid elements found in DOM');}else{console.warn('[Mermaid] Mermaid elements not found after waiting');}});}catch(e){console.error('[Mermaid] Error in init:',e);}};const readyState=document.readyState;console.log('[Mermaid] Current readyState:',readyState);if(readyState==='loading'){console.log('[Mermaid] Waiting for DOMContentLoaded');document.addEventListener('DOMContentLoaded',init);}else{console.log('[Mermaid] DOM already ready, calling init immediately');init();}})();"
  }
}
